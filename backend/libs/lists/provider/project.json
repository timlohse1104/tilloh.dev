{
  "name": "lists-provider",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/lists/provider/src",
  "projectType": "library",
  "tags": ["scope:lists", "type:provider"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/lists/provider/jest.config.ts"
      }
    }
  }
}
