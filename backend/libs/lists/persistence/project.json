{
  "name": "lists-persistence",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/lists/persistence/src",
  "projectType": "library",
  "tags": ["scope:lists", "type:provider"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/lists/persistence/jest.config.ts"
      }
    }
  }
}
